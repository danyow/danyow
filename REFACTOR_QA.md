# Retypeset 迁移验证

验证日期：2026-09-12。实际浏览器测试与构建在 GitHub Actions 运行 `34671723125` 中完成；构建通过后才提交生成的代码。此记录不将本地/构建测试等同于正式域名已经上线。

## 已验证

- 63 项 Node 自动测试全部通过，包含原有归档/中转测试及新增内容迁移测试。
- 101 个 HTML 页面成功生成；构建内站内链接、静态资源引用和日报回执校验通过。
- Astro 类型检查：0 errors、0 warnings、1 hint（旧分页重定向的未使用导入提示，不影响运行）。
- 对比迁移前源码：`note/`、`docs/`、`blog/`、`ai-engine-watch/reports/` 下共 63 个文件字节内容不变。
- 已保留内容：30 篇笔记、1 篇文档、28 篇公开旧博客、2 篇日报。另有 1 篇原本标记 unpublished 的博客源码保留，但不加入网站。
- 旧站内容路由对照：排除已退役 Unity/Lua、未发表草稿、分类页后，63 个旧内容/列表路径均有对应输出。旧博客分页和归档入口跳转到新列表。
- Chromium 实测宽度 320、390、1024、1440：分别访问首页、日报、笔记、代码文章，16 次访问均返回 200、默认暗色、无水平溢出。
- 四个尺寸均验证搜索 PWM 至少命中两篇、主题切换后刷新保留选择、无 JavaScript 页面错误。
- 已查看 390 与 1440 宽度的真实浏览器首页/文章截图。

## 发布验证

正式流水线使用 npm 锁文件，不再下载或生成 Unity 文档。发布沿用 gh-pages。发布后程序实际请求域名首页、笔记与文档、最近日报及原稿/回执，并比对本次构建的版本。正式部署结果以主分支 Actions 的 Verify public pages and report versions 为准。

测试产物中没有用户投递地址、令牌或 Google 账户配置。没有重新发送日报或修改每日任务时间。
